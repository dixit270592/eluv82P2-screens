/** Single UI stack — use everywhere instead of ad-hoc font families. */
export const UI_FONT_STACK = "'Instrument Sans', system-ui, sans-serif";

/** Plain family list for SVG/Recharts `fontFamily` props (no CSS-style wrapping quotes). */
export const UI_FONT_FAMILY = "Instrument Sans, system-ui, sans-serif";
